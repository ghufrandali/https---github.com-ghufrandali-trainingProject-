import { ViewDepNamePipe } from './view-dep-name.pipe';

describe('ViewDepNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ViewDepNamePipe();
    expect(pipe).toBeTruthy();
  });
});
